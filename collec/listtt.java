package collec;


import java.util.Collection;
import java.util.Collections;
import java.util.Iterator;
import java.util.List;



/*1.LIST interface can be used to insert the elements or objects in a sorted way inside, duplicates are allowed.
--- these are different classes that implemented list interface(array list, linked list, (vector,stack are  legacy classes means old one)
 -- in list insertion order preserved
---duplicates are also allowed
---null values are allowed
the methods that are present in the collection interface are also present in list interface.

-                       ------METHODS-------
1.add (object0)  , add(index ,object) //singel object adding
2.add all (collection c)              //to add group of objects
3.remove(object 0)
4.remove all(collection c)
5.retain all(collection c) except this group remaining all are removed
6.clear()7.is empty() 8.size() 9.contains(object) 10.contains all(collection c) 11.to array(collection c)
12.collections.sort(al--reference variable); ---to do soritng same types of dattatypes should be present
13.collections.shuffle()----shuffle all the objects in arraylist


list(i) have its own methods
--as the duplicates are allowed in list(i) index number play important role.

-------------METHODS OF LIST-------------

 1.add(index,object )
 2.add all(index,collection c)
 3.remove(index)
 4.get(index)--retain object or element in place of existed object
 5.set(index)-this method will replace new object or elemenet in place of existing object

 this methods are presnet in collection and list(interfaces) and implemented by the array and linked list classes
 



 
 --in linked list every object or element is called node(that has 3 parts)
 
 1.adress of previous elemet 2.index3.adress of next element
 
 
 *  ----------------LINKED LIST-------------
 *  linked list a=new linked list();-hetero data
 *  linked <integer>=new linked list<integer>();---homo
 *  */

public class listtt {

	
	
	public static void main(String[] args) {
		
	}
}